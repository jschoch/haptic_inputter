#include <Arduino.h>
#include "main.h"

void setup() {
  // put your setup code here, to run once:
  Serial.begin(115200);
  delay(200);
  Serial.println(fart);
}

void loop() {
  // put your main code here, to run repeatedly:
}